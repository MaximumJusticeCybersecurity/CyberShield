CyberShield Executive OS
Current implemented build: V60.3.24 TrustMap Render Lifecycle Controller
Build date: 2026-06-02

Purpose for bots and AI readers:
This file provides machine-readable context for CyberShield separate from the human-facing interface.

Current user-facing navigation:
Briefing, TrustMap, Runtime, Evidence, Proof Pack, Architecture, Settings.

No new top-level tabs were added for V60.3.24.

Public naming rule:
Public product name is CyberShield Executive OS. Current prototype build is V60.3.24. Do not call the public build CyberShield OS v8 unless the repo, README, loader, Settings/admin metadata, and public UX intentionally move to that version scheme.

Current TrustMap visual and performance stack:
- V60.3.12 = PNG asset mapping and interaction recovery
- V60.3.13 = stoplight trust color and PNG path recovery
- V60.3.14 = connector trust-state preservation and chain loader
- V60.3.16 = centerline fiber connector overlay and three-pane separation
- V60.3.16.1 = Trust Kernel right-panel detail and stoplight-only risk rows
- V60.3.17 = Briefing TrustMap Snapshot image
- V60.3.20 = consolidated Layer 1 visual consistency and view-mode recovery
- V60.3.21 = mobile load performance gate, TrustMap lazy-load trigger, mobile animation/filter reduction
- V60.3.22 = TrustMap PNG image prewarm after shell readiness
- V60.3.23 = TrustMap asset manifest and governed future asset intake
- V60.3.24 = TrustMap render lifecycle controller

V60.3.24 scope:
V60.3.24 adds one traceable lifecycle path for TrustMap request, stack load, asset manifest load, image prewarm, render detection, view-mode changes, and visual stabilization. It does not redesign the TrustMap or solve asset file size. Slow current images remain an asset optimization issue.

Runtime import rule:
The operational layer should not statically import the full TrustMap stack during initial page load. The executive shell should load first. The full interactive TrustMap loads on demand.

Lifecycle rule:
TrustMap render state should be tracked through lifecycle events and admin metadata rather than scattered timing assumptions. V60.3.24 introduces the lifecycle controller but does not yet refactor every historical timer out of prior modules.

Lifecycle events:
- cybershield:trustmap-requested
- cybershield:trustmap-stack-load-started
- cybershield:trustmap-stack-loaded
- cybershield:trustmap-asset-manifest-loaded
- cybershield:trustmap-images-prewarm-started
- cybershield:trustmap-images-prewarmed
- cybershield:trustmap-render-detected
- cybershield:trustmap-view-mode-changed
- cybershield:trustmap-visual-stabilized

Asset manifest rule:
TrustMap assets should be governed by `data/trustmap/v60-3-23-asset-manifest.json`. Current PNG files remain valid fallbacks. Future rebuilt black-background all-blue cube assets should be added through the manifest rather than hardcoded in scattered modules.

Image prewarm rule:
Do not force image prewarm into the critical startup path. Start prewarm after shell readiness. Prefer manifest paths. Warm the CyberShield Trust Kernel first, then Layer 1 PNGs and Briefing snapshot. If images remain slow, compress/resize PNG assets or add WebP/AVIF variants before adding more runtime logic.

Future asset intake rule:
- Use one locked template for canvas size, cube scale, camera angle, lighting, and margins.
- Use black or near-black backgrounds.
- Keep base art neutral blue/white/black.
- Green/yellow/red remain UI trust-state overlays only.
- Provide web-ready PNG and preferably WebP variants.

Performance doctrine:
- Load the executive shell first.
- Do not force mobile users to pay the full TrustMap cost during startup.
- Warm TrustMap images after the shell is usable.
- Load the full interactive TrustMap only when requested.
- Track render lifecycle before attempting more visual fixes.
- If the TrustMap remains slow because images are heavy, optimize files before adding more runtime logic.

Required current PNG assets in root assets folder:
- assets/CyberShield Trust Kernel.png
- assets/cloud_infrastructure.png
- assets/identities_access.png
- assets/applications_data.png
- assets/AI_systems_and_Agents.png
- assets/devices_endpoints.png
- assets/CMMC_and_Compliance.png
- assets/Third Parties and Vendors.png
- assets/The Trust Map.png

Non-negotiable TrustMap scope rule:
- Do not replace the TrustMap page unless explicitly directed as a refactor.
- Do not remove the left Operational Trust Score panel.
- Do not remove the right-side score/detail area.
- Do not remove Layer 2 or Layer 3.
- Do not replace the radar / constellation environment.
- Do not override #v554World transform with CSS.
- Do not create a second movement system.
- Trust-state colors must be stoplight green, yellow, or red only.
- Do not make the Briefing snapshot replace the full TrustMap page.

Controlling scoring doctrine:
CyberShield must not show decorative scores. Every score must trace to a named, versioned model with factors, weights, evidence inputs, confidence logic, missing evidence, assumptions, risk if wrong, and action consequence.

Guiding principle:
No score without a model. No model without evidence. No evidence without traceability. No action without consequence.

Prototype boundary:
The current GitHub Pages build is a lightweight static advisory prototype. It does not perform live enterprise integrations, live retrieval, live scoring, statistical validation, backend persistence, workflow automation, production enforcement, certification, or compliance determination.

Next builder first action:
Run browser QA on V60.3.24 from a phone. Verify shell speed, TrustMap on-demand loading, manifest fallback, current asset loading, lifecycle metadata, view controls, and no new UI clutter.
