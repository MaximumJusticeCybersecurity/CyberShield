# Rollback

Restore the previously pinned package version and SHA-256, rerun the consumer contract suite, and revert the exact package commit.  Do not delete evidence of failed validation.  Because the package has no network, credential, or execution authority, rollback affects only generated in-memory or local decision-record structures.
